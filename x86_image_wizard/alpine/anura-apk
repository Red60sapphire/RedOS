#!/bin/sh

if [ "$1" = "pre-commit" ]; then
    echo Running with red integration
elif [ "$1" = "post-commit" ]; then
    echo Updating linux bzimage and initrd on red fs...
    cp /boot/vmlinuz-virt /root/boot/bzimage
    cp /boot/initramfs-virt /root/boot/initrd.img
    echo Queuing virtual hda save...
    echo -ne "red.x86.virt_hda.save();\0" > /dev/hvc1
fi
exit 0